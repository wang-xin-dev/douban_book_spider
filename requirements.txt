# ============================================================
# 豆瓣高分图书爬虫项目 - 依赖清单
# ============================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 各依赖说明：
#   requests      - 发送 HTTP 请求，获取网页内容
#   beautifulsoup4 - 解析 HTML 页面，提取结构化数据
#   pandas        - 数据处理、清洗、导出 Excel
#   matplotlib    - 绘制数据可视化图表
#   lxml          - BeautifulSoup 的高性能 HTML 解析器
#   openpyxl      - pandas 导出 .xlsx 文件的底层引擎
# ============================================================

requests>=2.28.0          # HTTP 请求库（网页抓取核心）
beautifulsoup4>=4.11.0    # HTML 解析库（页面内容提取）
pandas>=1.5.0             # 数据处理库（清洗 + 导出 Excel）
matplotlib>=3.6.0         # 数据可视化库（绘制统计图表）
lxml>=4.9.0               # 高性能 XML/HTML 解析器（bs4 底层引擎）
openpyxl>=3.0.0           # Excel 文件读写引擎（pandas .xlsx 导出依赖）
